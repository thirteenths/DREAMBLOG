import { InjectionFactory } from "../injector/injection-factory";
/**
 * This service is an adapter for a resolved promise.
 */
export declare class ResolvedPromiseFactory implements InjectionFactory {
    constructor();
    factory(): <T>(value: T) => Promise<T>;
}
