import { InjectionFactory } from "../injector/injection-factory";
/**
 * This service is an adapter for a rejected promise.
 */
export declare class RejectedPromiseFactory implements InjectionFactory {
    constructor();
    factory(): <T>(value: T) => Promise<T>;
}
