import { PresetHasInOperatorExplorer } from "./preset.has-in-operator.explorer";
import { Presets } from "../../presets/presets";
/**
 * @hidden
 */
export declare class InOperatorInteractionExplorer {
    private presets;
    private explorer;
    constructor(presets: Presets<unknown>, explorer: PresetHasInOperatorExplorer);
    has(name: PropertyKey): boolean;
}
