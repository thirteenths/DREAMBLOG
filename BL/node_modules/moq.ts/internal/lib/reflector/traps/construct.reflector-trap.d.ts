import { Expressions } from "../expressions";
import { InjectionFactory } from "../../injector/injection-factory";
import { IReturnValueFactory } from "../expression-reflector";
export declare class ConstructReflectorTrap implements InjectionFactory {
    private readonly returnValueFactory;
    private readonly expressions;
    constructor(returnValueFactory: IReturnValueFactory, expressions: Expressions<unknown>[]);
    factory(): (target: any, args: any) => any;
}
