import { InteractionFormatter } from "./interaction.formatter";
import { Expression } from "../reflector/expressions";
/**
 * @hidden
 */
export declare class TrackedExpressionsFormatter {
    private interactionFormatter;
    constructor(interactionFormatter: InteractionFormatter);
    format(trackedExpressions: Expression[]): string;
}
