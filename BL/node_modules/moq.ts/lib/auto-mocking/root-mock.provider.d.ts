import { IMock } from "../moq";
import { InjectionFactory } from "../injector/injection-factory";
/**
 * @hidden
 */
export declare class RootMockProvider implements InjectionFactory {
    private readonly mock;
    private readonly root;
    constructor(mock: IMock<unknown>, root: IMock<unknown>);
    factory(): IMock<unknown>;
}
