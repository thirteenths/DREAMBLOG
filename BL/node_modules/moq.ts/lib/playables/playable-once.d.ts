import { IPlayable, PlayableUpdateReason } from "../moq";
/**
 * The configured setup will be applied only to the first invocation.
 */
export declare class PlayableOnce implements IPlayable {
    private played;
    readonly update: (reason: PlayableUpdateReason) => void;
    readonly isPlayable: () => boolean;
}
