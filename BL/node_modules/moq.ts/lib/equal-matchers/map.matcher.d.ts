import { IObjectMatcher } from "./object-matcher.type";
import { Injector } from "../static.injector/injector";
/**
 * Matches Map objects
 */
export declare class MapMatcher implements IObjectMatcher {
    private readonly injector;
    constructor(injector: Injector);
    matched<T = Map<unknown, unknown>>(left: T, right: T): boolean;
}
