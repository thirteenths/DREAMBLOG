import { IObjectMatcher } from "./object-matcher.type";
/**
 * Matches Date objects
 */
export declare class DateMatcher implements IObjectMatcher {
    matched<T = Date>(left: T, right: T): boolean;
}
