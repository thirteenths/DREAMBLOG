import { IterableTester } from "./iterable.tester";
import { IObjectMatcher } from "./object-matcher.type";
import { Injector } from "../static.injector/injector";
/**
 * Matches objects that support Iterable protocol
 */
export declare class IteratorMatcher implements IObjectMatcher {
    private readonly injector;
    private readonly iterableTester;
    constructor(injector: Injector, iterableTester: IterableTester);
    matched<T extends object>(left: T, right: T): boolean;
}
