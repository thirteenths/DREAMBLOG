import { InjectionFactory, TypeOfInjectionFactory } from "../../injector/injection-factory";
import { IMock, IPlayable, PromisedType } from "../../moq";
import { Presets } from "../presets";
import { Expressions } from "../../reflector/expressions";
import { RejectedPromiseFactory } from "../rejected-promise.factory";
/**
 * @Hidden
 */
export declare class MimicsRejectedAsyncPresetFactory<T, TValue = any> implements InjectionFactory {
    private readonly rootMock;
    private readonly presets;
    private readonly rejectedPromise;
    constructor(rootMock: IMock<T>, presets: Presets<T>, rejectedPromise: TypeOfInjectionFactory<RejectedPromiseFactory>);
    factory(): (target: Expressions<T>, playable: IPlayable, value: PromisedType<TValue>) => IMock<T>;
}
