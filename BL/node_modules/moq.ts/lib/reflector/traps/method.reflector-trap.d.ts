import { Expressions } from "../expressions";
import { InjectionFactory } from "../../injector/injection-factory";
export declare class MethodReflectorTrap implements InjectionFactory {
    private readonly expressions;
    constructor(expressions: Expressions<unknown>[]);
    factory(): (target: any, thisArg: any, args: any) => any;
}
